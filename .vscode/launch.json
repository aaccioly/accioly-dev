{
    "configurations": [
      {
        "type": "node",
        "request": "launch",
        "name": "Launch Hugo Server",
        "runtimeExecutable": "hugo",
        "args": [
          "server",
          "--buildDrafts",
          "--openBrowser"
        ],
        "cwd": "${workspaceFolder}"
      }
    ]
  }
